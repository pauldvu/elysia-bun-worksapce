{
  "workspaces": [
    "pkgs/*",
    "apps/*"
  ],
  "scripts": {
    "no": "bun i --no-save || echo 'runs bun with no save'",
    "d": "APP_ENV=local doppler run -- bun --watch run ./apps/platform/index.ts"
  },
  "dependencies": {
    "arktype": "^1.0.29-alpha",
    "hotscript": "^1.0.13",
    "string-ts": "^2.0.0"
  }
}
